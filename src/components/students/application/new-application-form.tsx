import { useForm } from "react-hook-form"

type Props = {}

export default function NewApplicationForm({ }: Props) {
    const form = useForm({

    })

    return (
        <div>NewApplicationForm</div>
    )
}